# Copy to .env in this directory (or set at the repo root — see root .env.example)
# for `prisma generate`/`migrate`/`studio` to resolve the datasource.
DATABASE_URL="postgresql://iecp:iecp@localhost:5432/iecp?schema=public"
