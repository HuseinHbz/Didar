# lib/shared

Reusable widgets and Dart extension methods used across more than one feature
module. Nothing here yet beyond this placeholder — add a `widgets/` or
`extensions/` file the first time a second feature needs to share something
`lib/core` doesn't already own.
